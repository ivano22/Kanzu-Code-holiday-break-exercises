/**
 * Write a program that checks if two variables have the same value and
 type.
 * 
 */
let x = '5';
let y = 5;
if (x === y) {
    console.log(`${y} and '${x}' have the same value and  data type`);
}
else {
    console.log(`${y} and '${x}' have the same value but different data types`);
}
