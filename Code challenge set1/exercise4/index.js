/**
 * 
 * Create a program that compares if 10is greater than or equal to 5.
 */

let x = 10;
let y = 5;
if (x >= y) {
    console.log(`${x} is greater than or equal to ${y}`);
}
else {
    console.log(`${y} is less than ${x}`);
}